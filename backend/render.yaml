services:
  - type: web
    name: waterpolo-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn projecte_waterpolo.wsgi:application
    envVars:
      - key: DJANGO_SETTINGS_MODULE
        value: projecte_waterpolo.settings